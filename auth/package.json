{
  "name": "example-auth",
  "version": "0.1.0",
  "private": true,
  "description": "Auth service: OIDC relying party that mints the role cookie; Varnish does the caching",
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "jose": "^6.0.0",
    "openid-client": "^6.3.0"
  }
}
