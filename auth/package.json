{
  "name": "example-community-app",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "description": "Community app stand-in: OIDC relying party that sets the domain-wide login cookie",
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "jose": "^6.0.0",
    "openid-client": "^6.3.0"
  }
}
