{
  "name": "example-edge",
  "version": "0.1.0",
  "private": true,
  "description": "Edge prototype: OIDC relying party + role-based cache in front of WordPress",
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "jose": "^6.0.0",
    "openid-client": "^6.3.0"
  }
}
